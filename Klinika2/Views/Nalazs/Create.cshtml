@model Klinika2.Models.Nalaz

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Nalaz</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PrijemID" class="control-label"></label>
                <select asp-for="PrijemID" class="form-control" asp-items="ViewBag.Prijemi">
                    <option value="">-- Odaberi Prijem --</option>
                </select>
                <span asp-validation-for="PrijemID" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="Opis" class="control-label"></label>
                <textarea asp-for="Opis" rows="10" class="form-control"></textarea>
                <span asp-validation-for="Opis" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
